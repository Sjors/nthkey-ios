source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby "2.7.2"

# nthKey.com
gem "jekyll"
gem "github-pages", group: :jekyll_plugins

# iOs (Travis doesn't have 1.10.1)
gem "cocoapods", "1.10.0"
